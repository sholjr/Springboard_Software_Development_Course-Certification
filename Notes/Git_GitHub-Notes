Git Handout --> https://lessons.springboard.com/GitHub-Handout-260f422b604880cc9fdcf8e42a0a1099


The command [git log] will show previous git commit history

In order to go back "in time" using Git the command will be [git reset --soft {commit id}}
[git reset --soft] Does not delete latest revisions, however it undoes changes allow a "step back" prior to the last commit
  
In order to go back to a pervious git commit, but you want to erase or "destroy" all changes the command us 
[git reset --hard {commit id}]

[git commit -m] will stage changes in [git status] and the "-m" flag allows for a custom message to be used to better recall your commit changes. 

When using the [git log] command press the "Q" key to exit the log and return to the terminal 

[git branch] shows how many branches are in the current git repository, branches allow mutiple users to collaborate on a signal project



